{
  "name": "@ui-organized/storybook-inspector",
  "version": "0.0.0",
  "private": true,
  "description": "Storybook addon (INSPECTOR.md) — a Figma-style properties panel that drives the real, live-rendered Storybook component via args. Reads props/variants from the shared manifest/components.json (same source as the Code Connect MCP server) and surfaces the same confidence/staleness model.",
  "type": "module",
  "main": "preset.ts",
  "exports": {
    "./preset": "./preset.ts",
    "./manager": "./dist/manager.js"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ui-organized/code-connect": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "storybook": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@ui-organized/tsconfig": "workspace:*",
    "esbuild": "^0.24.0",
    "react": "^18.0.0",
    "storybook": "^10.4.4",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
