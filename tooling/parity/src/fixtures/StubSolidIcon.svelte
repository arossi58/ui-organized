<!-- Solid cut of the fake icon library. See stubIconSet.ts. -->
<script lang="ts">
  let { size, strokeWidth }: { size?: number; strokeWidth?: number } = $props();
</script>

<svg data-cut="solid" data-size={size} data-stroke={strokeWidth}></svg>
