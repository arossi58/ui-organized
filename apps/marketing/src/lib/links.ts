/**
 * External destinations referenced across the site. Placeholders for now —
 * wire to the real deployed URLs when the builder/package/repo land.
 */
export const LINKS = {
  builder: "#",
  github: "https://github.com",
  npm: "https://www.npmjs.com",
} as const;
