---
"@ui-organized/react": patch
---

Update the `Meter` and `Progress` track background from `interactive-ui-default` to the theme-aware `interactive-secondary-default` overlay, following the design-token variable update (matches the `Avatar` fallback surface).
